# playwright-ts-arch-example

This has with main objective create an architecture to use the Playwright framework and a fast bootstrap for new projects.


### To run by UI mode 
```ENV=production npx playwright test --ui```